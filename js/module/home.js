define(
	['backbone', 'jquery', 'underscore', 'bootstrap'], 
	function(Backbone, $, _){

	var App = { View: {} };
	App.View = Backbone.View.extend({

		initialize: function(options){

			
        	
        	return this;
		}
	});

	return App;
});